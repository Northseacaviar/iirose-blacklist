# 成本账：本项目花了多少 token / 多少钱

**生成时间**：2026-09-25 20:26（北京时间）· **数据来源**：本机 Hermes 会话库 `D:/insane-robot/state.db`（**只读**查询）
**可复现**：`python tools/token-report.py`（同样的口径，命令行版）；本文的数字由脚本直查库生成，不手抄。

## 口径（怎么算的）

1. **全额计入**：本项目主会话（CLI `20260925_1634…`）+ 两个子 agent 独立审查会话（`source=subagent`，首条用户消息提到本项目）。
2. **按占比折算**：手机端 Telegram 那场（`20260925_1238…`）只有一部分在搞这个插件（其余在聊识图/语音/记忆容量），
   按含「拉黑/悬浮/屏蔽/blacklist」的消息行数占比（61/618 = **10%**）折算 —— 粗略近似，仅供横向比较。
3. `estimated_cost_usd` 是 Hermes 按当时价目表算的**估算值，不是账单**；`reasoning_tokens` 通常已含在输出口径里，
   所以四项 token 分开列、不重复相加。

## 明细

| 会话 | 开始 | 消息 | 工具调用 | 输入 | 输出 | 缓存读 | 推理 | 估算(USD) | 计入 |
|---|---|---|---|---|---|---|---|---|---|
| CLI 主会话（本项目主开发）（`20260925_1634…`）| 09-25 16:40 | 223 | 105 | 506,966 | 457,164 | 77,242,880 | 236,171 | 0.5821 | 全额 |
| 子 agent 独立审查（`20260925_1653…`）| 09-25 16:53 | 38 | 22 | 48,184 | 48,741 | 833,920 | 40,644 | 0.0390 | 全额 |
| 子 agent 独立审查（`20260925_1906…`）| 09-25 19:06 | 99 | 55 | 80,607 | 52,565 | 3,632,640 | 36,464 | 0.0545 | 全额 |
| Telegram 手机端那场（部分相关）（`20260925_1238…`）| 09-25 12:38 | 10 | 4 | 70,233 | 25,898 | 3,291,337 | 14,525 | 0.0359 | 折算 10% |

## 合计

- 消息 **370** · 工具调用 **186**
- 输入 **705,990** · 输出 **584,368** · 缓存读 **85,000,777** · 推理 **327,804**
- 四项相加 = **86,618,939**（推理可能已含在输出里，别当独立增量）
- 估算花费 **$0.7115**（按 1 USD≈7.1 粗算约 5 元人民币）

## 怎么读这些数

- **缓存读占了 98%**：Hermes 每轮都要带上长上下文，命中缓存的部分单价远低于新输入 —— 所以「总 token 近亿」不等于「很贵」。
- **子 agent 是独立会话**、token 单独记账（上面两行），已并入合计；单轮审查约 $0.04–0.06，是主会话的十分之一量级。
- **主会话还在跑**，数字会涨；要当时准确值就重跑 `tools/token-report.py`。
- 真实花费以 provider 后台账单为准；本表用于比较**各阶段的开销结构**（读规范/写代码/测试/审查各占多少）。

## 参照：当天（2026-09-25）全部会话

当天 5 场会话合计：消息 695 · 工具调用 343 · 估算 $1.4502 —— 本项目约占其中一半（其余是 PPT、环境配置等无关会话）。
