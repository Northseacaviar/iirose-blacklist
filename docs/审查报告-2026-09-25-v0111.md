# 审查报告 —— v0.1.11「拉黑不删已有记录」（2026-09-25）

## 一、这次审查是什么

- **触发**：vibecoding 流程第 6 步（进度审查），一次行为变更改动完成后派子 agent 独立审查。
- **审查者**：独立子 agent（`deleg_51443efb`，624 秒、43 次工具调用），**只输出报告、未改动项目任何文件**（复核方式：`git status` 干净）。
- **被审对象指纹**：`src/iirose-blacklist.js` = `iirose-blacklist.js` = `release/iirose-blacklist.js`，sha256 `e13ca8ea…378a88`，73343 字节，提交 `118624e`（19:06:38）。
- **审查范围**：`keepHistory`（默认不删历史）/ `hideSession`（私聊会话条目隐藏开关）两个新配置的正确性、边界、回归；测试质量；文案与事实一致性。
- **审查者亲手做了什么**：跑核心单测（34/34）、起本地服务用真实 Chrome 跑 harness（35/35）、**变异测试 6 轮**（改坏源码看测试抓不抓）、四组合探针 25 条断言、跨 5 秒全扫的保留验证、面板真实鼠标点击 + 跨页重载落盘验证、布局测量（1280×720 / 390×240）。临时文件在 `D:\insane-robot\cache\scratch\bl-review\`。

## 二、审查结论

**无阻塞项，可交真机验收。** 中等 5 条、建议 5 条。

## 三、逐条复核（我的判定）

| # | 审查意见 | 我的判定 | 证据/处理 |
|---|----------|----------|-----------|
| ③-1 | `sweepMessages()` 早退（475 行）未被任何用例锁住；把 475 改坏 harness 仍全绿 | **方向对、风险说法过重 → 部分采纳** | 我自己复跑变异：MA（475 失效）= 39/39 未捕获 ✓ 与它一致。但复验发现**475 在保留历史模式下不可观测**：逐行行为门在 464 行，475 只是周期性全扫的性能捷径（跳过整段遍历）；475 失效后 464 仍保护，删不掉任何东西，两种模式行为完全一致。**真正的行为门**（464）由 W33 覆盖，本次又补 W40 做确定性覆盖 → MB（464 失效）→ **W33 + W40 双抓到** ✓。475 本身不另加用例（锁性能无意义） |
| ③-2 | 两个新开关没有任何用例真实点过；回调接错字段 harness 仍全绿 | **采纳** | 新增 W37：`realClick` 两个新开关 → 断言 `conf` + 方框勾 + `localStorage` 三处。复跑 MD（回调写错字段）→ **W37 抓到**（39/40）✓ |
| ③-3 | `unblock()` 文案「旧消息已删，不会恢复」与新默认矛盾 | **采纳** | 改为按当前模式分两种说法：保留历史时「已有记录一直保留着」；关掉保留时「之前被清掉的旧消息不会回来」 |
| ③-4 | API 传非布尔值被 truthy 强转，`setKeepHistory(0)` 静默推进不可逆删除 | **采纳** | 5 个 setter 全部改 `typeof v !== 'boolean'` → 忽略并返回当前值（绝不切到破坏性方向）；新增 W38 覆盖 4 种非法入参 + 布尔正常生效 |
| ③-5 | uid 含下划线且前缀"像 uid"时 DOM 清扫认错 uid、该行删不掉（既有问题） | **采纳（修主路径）** | `uidOfMessageNode` 改为优先信头像 `data-uid`，`data-id` 仅兜底；新增 W39。残留：节点**既无头像 data-uid 又含下划线**时仍会走兜底截断 —— 该形态在真机未见，留作已知项 |
| ④-1 | README/源码注释多处与新行为矛盾（含文件头仍写 v0.1.9） | **采纳** | 全部对齐：README 技术路线段、目录段、排障入口列表、源码文件头、DOM 段注释、计数器标签（`历史` → `历史/会话`，因为默认模式它统计的是会话条目隐藏） |
| ④-2 | `tests/真机自测.js` 缺"拉黑后旧消息仍在"这一项（正是 A2 要看的） | **采纳** | 新增 `__T.retained()` 与步骤提示 |
| ④-3 | 计数器语义漂移 | **采纳（最小改法）** | 标签改为 `历史/会话`；不拆两个计数器（收益低） |
| ④-4 | 发布件手工同步、无构建脚本、测试发现不了落后 | **采纳** | 新增 `tools/publish.js`（复制 + `--check` 校验）+ core 单测新增"发布件必须等于源码"（现在落后会被测试直接拦住）；README 新增「发布」一节写全流程 |
| ④-5 | 新增两行后面板内容顶到 `maxHeight:540`；极矮视口下面板高于屏幕 | **部分采纳** | `maxHeight` 改为随视口收缩（`min(540, innerHeight-40)`，下限 200px）；390×240 下底部按钮本就在视口外属既有情况，未另调 |

**审查者自报但未被我复跑的一项**：ME（保留设置失效）→ 它称 W9 抓到（34/35）。我未单独复跑，同族三条（MB/MC/MD）复跑结论与它一致，故采信。

## 四、变异测试总表（我复跑的结果）

| 变异 | 改坏什么 | 改前套件 | 改后套件 | 捕获者 |
|------|----------|----------|----------|--------|
| MA | `sweepMessages()` 早退（475）失效 | 39/39 | 39/39 **未捕获** | 无（**不可观测**：真正的行为门在 464） |
| MB | 逐行行为门（464）失效 | 40/40 | 38/40 | W33 + **W40（新增）** |
| MC | `hideSession` 门失效 | 40/40 | 39/40 | W35 |
| MD | 面板开关回调写错字段 | 40/40 | 39/40 | **W37（新增）** |

方法：`cp -r src tests tools` 到 `D:\insane-robot\cache\scratch\mut-check\`，每次只注入一处变异、起独立服务（8772）跑 harness，跑完删除。**项目源码全程未被改动**（变异只发生在临时副本）。

## 五、残留已知项（不阻塞）

1. 消息节点既无头像 `data-uid`、其 `data-id` 前缀又含下划线时，uid 兜底解析会截断 → 该行在"关掉保留历史"的旧模式下漏删。真机未见过此形态（站点消息行都带头像）。
2. 面板 `maxHeight` 540px 上限在极矮视口（如 390×240）下靠内部滚动吸收，底部按钮需滚动才可见（既有）。
3. 私聊窗口历史若由 HTTP/快照补齐（README「真机待验证项」S10），保留历史模式下不处理（符合需求）；关掉保留时能否删除取决于 `.msgholderBox` 是否覆盖私聊窗口 —— 仍待真机确认。
